# Build output
output/
build/

# Kernel source (shallow clone)
linux/

# DTS intermediate/build artifacts
dts/*.dtb
dts/*.dts.pre

# ISO images (too large for git)
# Ubuntu ISO stays in src/ after download for rebuilds (~4GB)
src/*.iso

# PXE TFTP boot files (large, built by build.sh)
pxe/**

# PXE netconsole log
pxe/netconsole.log
